group: tests
module: greetings-app
version: '1.0'

main-class: app.App

compile-libs-dir: build/comp
runtime-libs-dir: build/runtime
output-dir: build/out

dependencies:
  - greetings:
      path: greeting
